class pontualTarget:
    def __init__(self, name: str, x: float, y: float, velocity: float):
        self.name = name 
        self.x = x # meters
        self.y = y # meters
        self.velocity = velocity # m/s